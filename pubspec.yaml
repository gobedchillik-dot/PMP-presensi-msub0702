name: tes_flutter
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  lucide_icons: ^0.257.0
  flutter_animate: ^4.5.0
  iconsax: ^0.0.8
  firebase_core: ^3.8.0
  cloud_firestore: ^5.4.4
  firebase_auth: ^5.3.0
  image_picker: ^1.1.0
  firebase_storage: ^12.4.10
  flutter_image_compress: ^2.1.0
  path_provider: ^2.1.2
  path: ^1.8.3
  geolocator: ^13.0.2
  http: ^1.1.0
  provider: ^6.1.1 # Gunakan versi terbaru yang stabil
  intl: ^0.20.2    # Gunakan versi terbaru yang stabil
  syncfusion_flutter_charts: ^31.2.12
  shimmer: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
